import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.fungestcost',
  appName: 'fungest-mobile-cost',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
